<%= form_for(@login_form) do |f| %>
  <% if @login_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login_form.errors.count, "error") %> prohibited this login_form from being saved:</h2>

      <ul>
      <% @login_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game %><br>
    <%= f.text_field :game %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
